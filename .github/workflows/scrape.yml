#url: https://wuisp-rust-dev.github.io/univ-crawler/school-rss/sookmyung/rss.xml
#url: https://wuisp-rust-dev.github.io/univ-crawler/school-rss/dongduk/rss.xml
#url: https://wuisp-rust-dev.github.io/univ-crawler/school-rss/seoul/rss.xml

name: Scrape Notices

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build + Generate RSS (oneshot)
        run: cargo run --release -- --oneshot

      # ✅ 여기서 'public' 전체를 업로드해야 /school-rss/... 경로가 그대로 유지됩니다.
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

  deploy:
    needs: scrape
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
